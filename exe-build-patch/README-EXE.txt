EXE化手順

1. WindowsにPython 3.11以降をインストールします。
2. build_exe.batをダブルクリックします。
3. dist\記事自動作成ツール.exe が生成されます。
4. EXEをランキングデータベースのルートへ置きます。
5. 同じカレントディレクトリにinput.txtを置いてEXEを実行します。

注意: EXEの横ではなく「EXEを起動したカレント」のinput.txtを読みます。通常はプロジェクトルートで実行してください。
