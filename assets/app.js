let articles=[];const list=document.querySelector('#articleList'),popular=document.querySelector('#popularList');const card=a=>`<a class="card" href="${a.url}"><img src="${a.image}" alt="${a.title}のサンプル画像" loading="lazy"><div class="card-body"><span class="tag">${a.category}</span><h3>${a.title}</h3><span class="meta">更新 ${a.date} ｜ ${a.views.all.toLocaleString()} views</span></div></a>`;function render(items){list.innerHTML=items.map(card).join('');document.querySelector('#noResults').hidden=items.length>0}function popularRender(period){popular.innerHTML=[...articles].sort((a,b)=>b.views[period]-a.views[period]).slice(0,3).map(card).join('')}fetch('data/articles.json').then(r=>r.json()).then(d=>{articles=d;render(d);popularRender('all')}).catch(()=>{list.innerHTML='<p>ローカルサーバーで開いてください。</p>'});document.querySelectorAll('.tabs button').forEach(b=>b.addEventListener('click',()=>{document.querySelector('.tabs .active').classList.remove('active');b.classList.add('active');popularRender(b.dataset.period)}));document.querySelector('#searchForm').addEventListener('submit',e=>{e.preventDefault();let q=document.querySelector('#searchInput').value.trim().toLowerCase();render(articles.filter(a=>(a.title+' '+a.keywords).toLowerCase().includes(q)))});document.querySelector('.menu').addEventListener('click',e=>{let n=document.querySelector('.site-header nav');n.classList.toggle('open');e.currentTarget.setAttribute('aria-expanded',n.classList.contains('open'))});